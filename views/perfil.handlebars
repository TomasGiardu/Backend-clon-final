<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil de Usuario</title>
</head>
<body>
    <h1>Perfil de Usuario</h1>
    <h2>Datos del Usuario</h2>
    <p>Nombre: {{ user.first_name }} {{ user.last_name }}</p> <!-- Concatenación de first_name y last_name -->
    <p>Rol: {{ user.role }}</p>

    <h2>Subir Producto</h2>
    <form action="/{{user.id}}/documentos" method="POST" enctype="multipart/form-data">

        <label for="productImage">Imagen de Producto:</label>
        <input type="file" id="productImage" name="productImage"><br><br>

        <label for="documentText">Datos:</label>
        <input type="text" id="documentText" name="documentText" placeholder="Ingrese los datos del producto" style="width: 300px;"><br><br>

        <button type="submit">Subir Archivos</button>
    </form>

    <a href="/realTimeProducts">
        <button>volver</button>
    </a>
</body>
</html>
